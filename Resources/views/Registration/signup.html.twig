<form action="{{ path("TeapotioBaseUserBundle_signup") }}" method="post" {{ form_enctype(form) }}>
  {{ form_errors(form) }}

  {{ form_widget(form) }}
  <button>{% trans %}user.signup{% endtrans %}</button>
</form>